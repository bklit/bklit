{
  "name": "@bklit/create",
  "version": "1.0.9",
  "description": "Create a new Bklit instance with one command",
  "type": "module",
  "bin": {
    "create": "./dist/index.js"
  },
  "files": [
    "dist"
  ],
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "build": "tsup src/index.ts --format esm --clean",
    "dev": "tsup src/index.ts --format esm --watch"
  },
  "dependencies": {
    "commander": "^12.1.0",
    "inquirer": "^13.1.0",
    "chalk": "^5.3.0",
    "ora": "^8.1.1",
    "execa": "^9.5.2"
  },
  "devDependencies": {
    "@bklit/tsconfig": "workspace:*",
    "tsup": "^8.3.5",
    "typescript": "catalog:",
    "@types/inquirer": "^9.0.7",
    "@types/node": "^22.10.5"
  }
}
